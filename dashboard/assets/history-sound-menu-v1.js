/*
 * XLX Modern — public navigation no longer exposes the Bip proxy button.
 *
 * Audio/beep functionality remains available through the panel's own audio
 * and accessibility controls. This compatibility file intentionally performs
 * no DOM mutation so existing index.php references remain harmless.
 */
(() => {
  'use strict';
})();
