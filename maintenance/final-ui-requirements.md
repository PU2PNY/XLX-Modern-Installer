# XLX026 — Final UI requirements

Before closing the XLX026 maintenance/update project, apply the following footer requirement to every public dashboard page, preserving the current visual layout and responsive behavior:

- Show the released dashboard version using semantic versioning (`vX.Y.Z`).
- Provide a public GitHub link to the XLX026 dashboard source repository or canonical dashboard source location.
- Provide a public GitHub link to `PU2PNY/XLX-Modern-Installer`.
- Keep the footer compact, accessible, responsive, and visually unobtrusive.
- Do not invent a dashboard repository URL: the dashboard link must point to an actually published source location.
- Apply only after core/update validation is complete and after backing up the current public web tree.

Suggested visible format:

`Painel XLX026 vX.Y.Z · Código do Painel · Instalador XLX Modern`

The exact dashboard version will be assigned at the final release stage, after the production state is validated.
