name: Sync smoke test

on:
  push:
    branches:
      - 'sync/production-20260813'
    paths:
      - '.sync-payload/TEST'

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo OK
